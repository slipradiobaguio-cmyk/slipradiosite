{
  "name": "slip-radio-site",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --kv SHOWS --kv CHAT_RL --kv SUBMISSIONS --r2 THUMBNAILS --d1 CHAT",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
